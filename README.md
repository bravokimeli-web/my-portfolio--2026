after configurations of myportfolio, I build the image, pushed to ecr then deploy it using ecs 
The second and recommended way I use is deployed it in an S3, use cloudfront for CDN and route53 for the domain
